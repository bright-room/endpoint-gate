---
name: plan
description: GitHub Issue を参照して実装プランを作成する。ローカルでファイルとして出力する。
argument-hint: "[issue-number]"
---

# Implementation Plan Skill

GitHub Issue を参照し、コードベースを調査した上で実装プランを作成する。

## 前提条件

- `gh` CLI が認証済みであること

## 手順

### 1. Issue の取得

引数に Issue 番号を指定する（必須）。

`gh issue view <issue-number>` で Issue のタイトル、本文、ラベル、コメントを取得し、要件を正確に理解する。

### 2. コードベースの調査

Issue の要件に基づいて、関連するコードを徹底的に調査する。

#### 2-1. モジュール構成の把握

`settings.gradle.kts` を読み込み、現在のモジュール一覧を把握する。`gradle-scripts/` 配下の convention plugin を確認し、利用可能なプラグイン（ビルド慣習、テスト構成等）を把握する。

#### 2-2. 既存パターンの調査

- **同種の実装**: 要件に最も近い既存モジュールを特定し、そのディレクトリ構成・クラス設計・テスト構成をリファレンスとする
- **階層パターン**: 変更が core 層の SPI に影響する場合、対応する非同期 adapter 層（reactive-core 等）やフレームワーク層への波及を確認する
- **拡張ポイント**: Auto-configuration、SPI、Bean 登録条件（`@ConditionalOnMissingBean` 等）の活用箇所を確認する

#### 2-3. テスト構成の調査

対象モジュールの `build.gradle.kts` を読み込み、適用されている convention plugin からテスト構成（ソースセット、実行タスク）を把握する。

### 3. 実装プランの作成

以下の観点で実装プランを設計する:

- **実現可能性**: 要件が現在のアーキテクチャで実現可能かを検証する
- **設計判断**: 複数のアプローチがある場合は、各アプローチのトレードオフを分析する
- **段階的な実装**: 変更を論理的なステップに分割し、各ステップの依存関係を明確にする
- **テスト戦略**: 各変更に対するテスト方針を策定する

### 4. 実装プランの出力

実装プランを `.claude/outputs/plans/` ディレクトリにファイルとして出力する。

- ディレクトリが存在しない場合は作成すること
- ファイル名: `PLAN-<Issue番号>-<Issueタイトルをケバブケースに変換>.md`
  - 例: Issue #42 "Add WebFlux support" → `PLAN-42-add-webflux-support.md`
  - タイトルに含まれる特殊文字は除去し、スペースをハイフンに変換する

## 実装プランのフォーマット

以下のフォーマットに従って Markdown を生成する。

````markdown
## 実装プラン

> 📋 Issue: #<issue-number>
> 📅 作成日: YYYY-MM-DD

### 概要

<!-- Issue の要件を簡潔に要約し、実装方針の全体像を述べる -->

### 設計判断 *(省略可: 複数のアプローチがある場合のみ記載)*

| アプローチ | メリット | デメリット |
|-----------|---------|-----------|
| A: <方式名> | <メリット> | <デメリット> |
| B: <方式名> | <メリット> | <デメリット> |

**採用案**: <選択したアプローチとその理由>

### 影響範囲

<!-- settings.gradle.kts から読み取ったモジュール一覧に基づき、
     変更の影響があるモジュールのみ行を作成する。
     影響のないモジュールは記載不要。 -->

| モジュール | 影響 | 備考 |
|-----------|------|------|
| `<module-path>` | 新規 / 変更 | <概要> |

### ファイル構成

<!-- 対象モジュールの既存パッケージ構造に従ってパスを記述する。
     パッケージ命名規則は既存モジュールから導出すること。
     新規追加は (new)、変更は (modify) で表記する -->

```
module/src/main/java/...
├── subpackage/
│   ├── NewClass.java          (new)
│   └── ExistingClass.java     (modify)
└── another/
    └── AnotherNewClass.java   (new)
```

### 実装ステップ

<!-- 実装を Phase（論理的なまとまり）と Step（具体的な作業単位）に分割する -->

#### 実装ステップの設計指針

- **core 層の変更**: SPI の追加・変更は、対応する非同期 adapter 層（reactive-core 等）への対称的な変更が必要か検討する
- **フレームワーク層の変更**: Auto-configuration の登録順序、Bean 登録条件（`@ConditionalOnMissingBean` 等）の設計を明記する
- **新モジュールの追加**: `settings.gradle.kts` への追加、適用する convention plugin の選定、依存グラフ上の位置づけを明記する

#### Phase 1: <フェーズのタイトル>

<!-- フェーズの目的・ゴールを簡潔に述べる -->

##### Step 1: <ステップのタイトル>

**対象**: `module/path/to/File.java`

<!-- 具体的な変更内容を記述する。必要に応じてコード例を含める -->

```java
// コード例（必要な場合のみ）
```

##### Step 2: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

#### Phase 2: <フェーズのタイトル>

##### Step 1: <ステップのタイトル>

<!-- 同様のフォーマットで記述 -->

### ドキュメント更新

<!-- 実装に伴い更新が必要なドキュメントを記載する -->

| ドキュメント | 更新内容 |
|-------------|---------|
| `README.md` | <更新内容の概要> |
| `CLAUDE.md` / `.claude/rules/architecture.md` | <アーキテクチャ変更がある場合> |
| `<module>/src/main/resources/META-INF/additional-spring-configuration-metadata.json` | <設定プロパティ追加時> |
| `<module>/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` | <Auto-configuration 追加時> |

### テスト戦略

<!-- 対象モジュールの build.gradle.kts で適用されている convention plugin に基づき、
     利用可能なテスト構成（ソースセット、実行タスク）を反映する -->

| テスト種別 | ソースセット | 対象 | テスト内容 |
|-----------|-------------|------|-----------|
| ユニットテスト | `src/test/java` | `ClassName` | <テスト内容> |
| 統合テスト | `src/integrationTest/java` | `ClassName` | <テスト内容> |

### 確認事項 *(省略可: 確認すべき事項がなければセクションごと省略)*

- [ ] <確認事項1>
- [ ] <確認事項2>

### 今後の展望 *(省略可: スコープ外の関連拡張がなければセクションごと省略)*

- **<拡張テーマ>**: <概要と期待される効果>
- **<拡張テーマ>**: <概要と期待される効果>

---
🤖 *Generated by Claude Code*
````

## 注意事項

- Issue の要件を正確に理解し、過不足のないプランを作成すること
- 推測ではなく、実際にコードを読んで確認した事実に基づいてプランを作成すること
- 実装ステップは具体的に記述し、各ステップで変更するファイルパスを明示すること
- 既存のアーキテクチャパターンや命名規則に従った設計にすること
- 不明点や判断が必要な事項は「確認事項」セクションに明記すること
- 変更のない「設計判断」「確認事項」「今後の展望」セクションは省略してよい
- 「今後の展望」は Issue のスコープ外の関連拡張を記載する。実装プラン策定時にコードベースを調査する中で見つけた改善機会や、自然な次のステップとなる機能拡張を挙げること
- 実装に伴うドキュメント更新を必ず「ドキュメント更新」セクションに記載すること。新機能・設定変更・アーキテクチャ変更がある場合、対応するドキュメントの更新箇所を具体的に特定すること
